name: Projeto CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: clean 
      run: cd projeto && make clean
    - name: make
      run: cd projeto && make
